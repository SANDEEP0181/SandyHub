# AI Provider Adapters

Future adapters should implement a common interface for generation, provider name, and supported models.

Provider credentials belong only in server-side environment variables. Never place API keys in HTML, CSS, client-side JavaScript, GitHub Pages, or committed config files.